#include<stdio.h>
int main()
{
    int num;
    printf("enter a num:");
    scanf("%c,&num");
    if(num>0)
    printf("positive num");
    else if(num<0)
    printf("negative num");
    else
    printf("zero");
    return 0;
}